@model IEnumerable<Hart_PROG7311_Part_2.Models.ProductModel>

@inject IHttpContextAccessor HttpContextAccessor

@{
    FarmerModel farmer = null;
    ViewData["Title"] = "View";
    if (ViewData["Farmer"] != null)
    {
        farmer = ViewData["Farmer"] as Hart_PROG7311_Part_2.Models.FarmerModel;
    }
    else
    {
        farmer = new FarmerModel();
    }
}

<div class="container mt-4">
    <div class="row">
        <!-- Left Side - Profile Details -->
        <div class="col-md-4">
            <div class="card profile-card">
                <div class="card-body text-center">
                    <img src="/@farmer.ProfilePicture" alt="Profile Image" class="rounded-circle profile-image mb-3">
                    <h3>@farmer.Name</h3>
                    <p>@farmer.Username</p>
                    <p class="text-muted">@farmer.Address>
                    <p class="profile-bio">@farmer.PhoneNumber</p>
                    <p class="text-muted">@farmer.CreateedAt>
                </div>
            </div>
        </div>

        <!-- Right Side - Products -->
        <div class="col-md-8">
            <div class="products-header">
                <h4>Products for Sale</h4>

                <!-- Filters -->
                <div class="product-filters mb-4">
                    <div class="row">
                        <div class="col-md-3">
                            <select class="form-control" id="sortBy">
                                <option value="date">Sort by: Newest</option>
                                <option value="price_asc">Price: Low to High</option>
                                <option value="price_desc">Price: High to Low</option>
                            </select>
                        </div>
                        <div class="col-md-3">
                            <select class="form-control" id="categoryFilter">
                                <option value="all">All Categories</option>
                                <option value="Liquid">Liquid</option>
                                <option value="Tools">Tools</option>
                                <option value="Produce">Produce</option>
                                <option value="Technolodgy">Technolodgy</option>
                                <option value="Livestock">Livestock</option>
                            </select>
                        </div>
                        <div class="col-md-3">
                            <button class="btn btn-outline-secondary btn-block" id="applyFilters">Apply</button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Product Grid -->
            <div class="product-grid">
                <div class="row">
                    @foreach (var product in Model)
                    {
                        <div class="col-md-4 mb-4">
                            <div class="card product-card">
                                <img src="/@product.Image" class="card-img-top product-image" alt="@product.Name">
                                <div class="card-body">
                                    <h5 class="card-title">@product.Name</h5>
                                    <p class="card-text text-muted">@product.Category</p>
                                    <div class="d-flex justify-content-between align-items-center">
                                        <span class="price">R @product.Price.ToString("N2")</span>
                                    </div>
                                </div>
                                <div class="card-footer bg-white">
                                    <a class="btn btn-sm btn-outline-primary btn-block" asp-area="" asp-controller="Product" asp-action="Details" asp-route-id="@product.ProductModelID">View Details</a>
                                </div>
                            </div>
                        </div>
                    }
                    @if (HttpContextAccessor.HttpContext.Session.GetString("UserType") == "Farmer")
                    {
                        <div class="col-md-4 mb-4">
                            <div class="card product-card">
                                <div class="card-body">
                                </div>
                                <div class="card-footer bg-white">
                                    <a class="btn btn-sm btn-outline-primary btn-block" href="@Url.Action("Create", "Product")">New Product</a>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            // Filter functionality
            $('#applyFilters').click(function() {
                const sortBy = $('#sortBy').val();
                const category = $('#categoryFilter').val();
                const searchTerm = $('#productSearch').val();

                window.location.href = `@Url.Action("FarmerIndex")?sortBy=${sortBy}&category=${category}`;
            });

            // Initialize filters from query parameters
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.has('sortBy')) {
                $('#sortBy').val(urlParams.get('sortBy'));
            }
            if (urlParams.has('category')) {
                $('#categoryFilter').val(urlParams.get('category'));
            }
            if (urlParams.has('search')) {
                $('#productSearch').val(urlParams.get('search'));
            }
        });
    </script>
}